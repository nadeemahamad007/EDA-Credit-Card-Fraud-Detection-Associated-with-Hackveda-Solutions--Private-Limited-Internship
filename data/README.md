# Dataset

Place the dataset file here as:

`data/creditcard.csv`

The original notebook expects the public Credit Card Fraud Detection dataset with the target column `Class`.

The raw CSV is intentionally ignored by Git because it can be large. Add the dataset locally when running the notebook/dashboard, and document the exact source/licensing terms you use before redistributing any copy.
